const express = require('express');
const router = express.Router();

// TODO: wire up controller functions for auth routes here
// const authController = require('../controllers/auth.controller');

module.exports = router;
